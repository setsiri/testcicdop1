/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "export",
  basePath: "/testcicdop1",
  images: {
    unoptimized: true,
  },

};

export default nextConfig;
